package simple.reboot.com.actions

enum class PowerActionType {
    POWER_OFF,
    RESTART,
    RESTART_SYSTEMUI,
    SOFT_RESTART,
    RESTART_RECOVERY,
    RESTART_BOOTLOADER,
    RESTART_SAFE_MODE
}